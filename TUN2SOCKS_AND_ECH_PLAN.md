# tun2socks (badvpn) — реализовано в v3.7. ECH — план на будущее

## 1. tun2socks (badvpn) — ✅ РЕАЛИЗОВАНО в v3.7 CIS-MAX

### 1.1 Что оказалось не так в предыдущем плане

Более ранняя версия этого документа утверждала, что нужен патч `--tunfd` в
badvpn (беря пример с ByeDPIAndroid/hev-socks5-tunnel). **Это было неточно.**
При реальном изучении исходников оказалось, что `badvpn/tuntap/BTap.h` уже
содержит нужный механизм "из коробки":

```c
enum BTap_init_type {
    BTAP_INIT_STRING,  // открыть /dev/net/tun по имени устройства (root)
#ifndef BADVPN_USE_WINAPI
    BTAP_INIT_FD,      // ← принять уже открытый fd напрямую!
#endif
};
```

`BTap_Init2()` с `BTAP_INIT_FD` — ровно то, что нужно: передать
`ParcelFileDescriptor.getFd()` из `VpnService.Builder.establish()` без
всякого патчинга. Отдельный патч `--tunfd` не понадобился.

### 1.2 Что было реально сделано

1. **Vendored badvpn** (не submodule, как и byedpi) в
   `app/src/main/jni/badvpn/` — обрезано с 12MB до ~4.6MB (убраны
   client/server/ncd/examples/dostest/win32-специфика, не нужные для
   tun2socks; список файлов взят из `compile-tun2socks.sh` — официального
   "быстрого Linux-билда" от автора badvpn).

2. **Патч `tun2socks.c`**: добавлен библиотечный режим
   `TUN2SOCKS_LIBRARY_MODE`, который:
   - оборачивает оригинальный CLI `int main(argc, argv)` в
     `#ifndef TUN2SOCKS_LIBRARY_MODE` (апстримный код НЕ тронут — сохраняет
     совместимость с будущими апдейтами badvpn);
   - добавляет `tun2socks_bridge_run(config)` / `tun2socks_bridge_stop()`
     (см. `tun2socks_bridge.h`) — переиспользует существующие
     `process_arguments()`, `terminate()` и весь event-handling код,
     заполняя `options.*` программно вместо `parse_arguments(argc, argv)`;
   - TUN открывается через `BTAP_INIT_FD` с `dup()`'нутым fd (оригинал,
     переданный из Kotlin, остаётся у `ParcelFileDescriptor` и закрывается
     Android-стороной как обычно);
   - остановка — через self-pipe, зарегистрированный в `BReactor` как
     обычный файловый дескриптор (тот же паттерн, что `BADVPN_USE_SELFPIPE`
     в `BUnixSignal.c`) — `BReactor` не защищён мьютексами (`NO_SYS=1`
     дизайн lwIP), поэтому вызвать `BReactor_Quit()` напрямую из другого
     потока небезопасно;
   - логирование через инжектируемый `Tun2SocksBridgeLogFunc`
     (`__android_log_print` на Android-стороне) вместо `BLog_InitStdout()`.

3. **`tun2socks_jni.c`** переписан: вместо пустого цикла `while(g_running)`
   вызывает `tun2socks_bridge_run()` в pthread, с fallback на прежнюю
   заглушку, если `BADVPN_AVAILABLE` не определён на этапе сборки.

4. **`CMakeLists.txt`**: `badvpn_tun2socks` собирается как статическая
   библиотека из ~50 .c-файлов (badvpn core + lwIP core, без api/apps/netif —
   не нужны при `NO_SYS=1`), с теми же флагами, что `compile-tun2socks.sh`
   (`BADVPN_USE_EPOLL`, `BADVPN_USE_SIGNALFD` — стандартные Linux/Bionic API,
   доступны на Android без изменений).

5. **`ciadpi_jni.c`**: включён `params.udp = true` — иначе SOCKS5 UDP
   ASSOCIATE (нужен для QUIC/DNS через tun2socks → `SocksUdpClient`)
   отклонялся бы byedpi на уровне `handle_s5()`.

### 1.3 Проверка

Полный путь **TUN → lwIP → BSocksClient → SOCKS5 → обратно** был
E2E-протестирован на этой машине (Linux x86_64):
1. Собран весь badvpn+lwIP набор + пропатченный `tun2socks.c` в
   `TUN2SOCKS_LIBRARY_MODE` — компилируется чисто, без предупреждений.
2. Создан реальный `/dev/net/tun`-интерфейс (`sudo`, т.к. локально нет
   Android-эмулятора), запущен `tun2socks_bridge_run()` в pthread.
3. Запущен минимальный Python SOCKS5-сервер (echo).
4. `nc 10.0.0.2 12345` отправил данные через TUN — фейковый SOCKS5-сервер
   получил `CONNECT` запрос и данные, эхо вернулось клиенту через тот же
   TUN. Оба направления (TUN→SOCKS5 и SOCKS5→TUN) подтверждены рабочими.
5. `tun2socks_bridge_stop()` корректно останавливает event loop без
   segfault/утечек (после исправления найденного и устранённого бага —
   `BLog_Free()` вызывал `free_func()` безусловно, для кастомного
   Android-логгера была нужна no-op-функция, а не `NULL`).

**Не проверено:** реальная сборка через Android NDK (недоступен в этой
рабочей среде — нет сети для скачивания NDK) и запуск на настоящем
Android-устройстве/эмуляторе. Компиляция под Bionic должна работать
идентично Linux x86_64 (те же `BADVPN_LINUX`/`BADVPN_USE_EPOLL` флаги,
никаких glibc-специфичных вызовов в задействованных файлах не найдено), но
это стоит подтвердить прогоном CI (`./gradlew assembleDebug`) и ручным
тестом на устройстве перед тем, как полагаться на этот путь в проде.

### 1.4 Известные ограничения после этой реализации

- ~~**IPv6 в ciadpi**: `params.ipv6 = false`~~ — **исправлено**. `ciadpi_jni.c`
  теперь настраивает `params.baddr` как dual-stack (`AF_INET6` +
  `in6addr_any`), `params.ipv6 = true`. `remote_sock()` в byedpi/proxy.c сам
  включает `IPV6_V6ONLY=0` на сокете назначения, когда семейство адреса
  назначения `AF_INET6` — этого достаточно, чтобы один и тот же baddr
  одинаково обслуживал и v4-mapped-v6 адреса (`map_fix(dst,6)` преобразует
  в них чистый IPv4 dst), и нативные IPv6-адреса. Проверено локально
  функциональным тестом, воспроизводящим точную логику `remote_sock()`/
  `map_fix()`: dual-stack сокет успешно подключается к обоим типам
  назначений и передаёт данные в обе стороны.
- **UDP fragmentation/MTU**: `tun2socks_bridge_run()` использует MTU=1500 по
  умолчанию; для мобильных сетей (LTE с меньшим MTU из-за инкапсуляции)
  может потребоваться подстройка — не протестировано на реальной мобильной
  сети.
- ~~**Split-tunnel self-exclusion bug**~~ — **исправлено**. В
  `BypassVpnService.runVpn()` раньше при непустом `allowedApps` вызывался
  `addAllowedApplication()`, а затем всё равно `addDisallowedApplication(
  packageName)` — Android бросал `UnsupportedOperationException` («Builder
  может иметь только allowed ИЛИ disallowed список, не оба»), которая молча
  проглатывалась. Теперь в allowed-режиме `addDisallowedApplication()` не
  вызывается вообще (`packageName` просто не входит в отфильтрованный
  allowed-список), а в disallowed-режиме (`allowedApps` пуст) оба вызова
  корректны, т.к. `addAllowedApplication()` в этой ветке не используется.

---

## 2. ECH (Encrypted Client Hello) — план остаётся актуальным

### 2.1 Почему это не "добавить флаг"

ECH — это не модификация ClientHello руками (как split/disorder в
PacketProcessor), а полноценная HPKE-операция:

1. Получить **ECH-конфигурацию** сервера — это TXT/HTTPS-запись DNS
   (`type65 HTTPS` с параметром `ech=...`) для домена. Наш `DohResolver`
   сейчас резолвит только A/AAAA — нужно добавить парсинг HTTPS/SVCB-записей
   через DoH (RFC 9460).
2. Сформировать **ClientHelloInner** (настоящий, с реальным SNI) и
   **ClientHelloOuter** (с публичным `public_name` вместо SNI) —
   зашифровать Inner через HPKE (`X25519 + HKDF-SHA256 + AES-128-GCM` — набор,
   который использует Chrome/Cloudflare) публичным ключом из ECH-конфига.
3. TLS-стек **должен уметь это делать сам** — вручную патчить байты
   ClientHello (как делает `PacketProcessor` для split) для ECH невозможно,
   потому что нужна полноценная HPKE-криптография и валидный TLS 1.3
   handshake state machine.
4. Единственный практичный путь на Android — использовать TLS-библиотеку с
   поддержкой ECH: **BoringSSL** (собранный с `--enable-ech`, как это делает
   Chrome/Cronet) — обычный Android `SSLEngine`/`javax.net.ssl` ECH не
   поддерживает. Значит: собрать `boringssl` под NDK (это тяжёлая, но хорошо
   документированная сборка, ~10-15 минут CI-времени), обернуть через JNI
   как `ssl_ech_jni.so`, и **весь TLS-трафик приложения должен идти через
   этот стек**, а не через системный `SSLEngine` — то есть фактически нужен
   пользовательский SNI-прокси (аналог `sslocal`/`v2ray`), который терминирует
   TLS от клиента... что для прозрачного VPN (TUN-режим, произвольные
   приложения) означает MITM внутри самого VPN-сервиса, а это отдельный
   архитектурный слой (TLS-интерцептирующий прокси на локальном сертификате),
   не совместимый с "трафик не покидает устройство и не расшифровывается"
   принципом текущего README.

### 2.2 Более реалистичная альтернатива для того же результата

Так как цель ECH — "скрыть SNI от DPI", а не именно ECH-протокол как таковой,
дешевле и быстрее по времени реализации (без BoringSSL-сборки) дать 80% эффекта:

- **Domain fronting через DoH HTTPS-записи** (получаем "front" домен с высокой
  репутацией, не блокируемый DPI, и коннектимся туда с реальным SNI внутри
  зашифрованного TLS record — актуально там, где DPI смотрит только на SNI,
  а не делает полный ECH-детект).
- **oobPosition/tlsRec фрагментация SNI-байтов** (уже реализовано в ciadpi
  через `-o`/`-r`, активно во всех текущих пресетах) — рвёт сигнатуру SNI
  без полноценного ECH.

Эти меры уже частично покрыты существующими пресетами (`oobPosition`,
`tlsRec`). Настоящий ECH стоит планировать отдельным релизом v4.0 с явным
выделением времени на сборку BoringSSL под NDK и тестирование на реальном
трафике.

### 2.3 Рекомендация

tun2socks (п.1), split-tunnel self-exclusion и IPv6 в ciadpi уже реализованы
и E2E-протестированы (в разной степени — см. таблицу ниже). Следующим
логичным шагом перед ECH является:
1. Реальная сборка через Android NDK (подтверждена зелёным CI-прогоном на
   GitHub Actions) + ручной прогон на реальном устройстве/эмуляторе —
   подтвердить, что путь работает не только на Linux x86_64 и в CI, но и
   при реальном использовании конечным пользователем.
2. Только затем — отдельная ветка `feature/ech` с CI-джобой, которая явно
   собирает BoringSSL (добавит 10-15 минут к каждому CI-прогону,
   нежелательно мешать с основной веткой, пока не стабилизировано).

---

## 3. КРИТИЧЕСКИЙ баг: краш всего приложения при рестарте tun2socks (v3.7.2)

### 3.1 Симптом (найден пользователем на реальном устройстве)

При первом реальном тесте на устройстве: приложение запускается, VPN
включается (foreground-уведомление появляется), но через несколько секунд
приложение "сворачивается" само по себе, как будто закрылось, а VPN при
этом не работает (уведомление может продолжать висеть какое-то время как
артефакт системы).

### 3.2 Причина

`tun2socks_bridge_run()` вызывает `BNetwork_GlobalInit()` и `BTime_Init()` —
обе функции унаследованы из апстримного badvpn и содержат:

```c
ASSERT(!bnetwork_initialized)   // BNetwork.c
ASSERT(!btime_global.initialized)  // BTime.h
```

В апстримном CLI-режиме tun2socks эти функции вызываются **ровно один раз**
за время жизни процесса (при старте, до `BReactor_Exec()`), поэтому проблема
никогда не проявлялась в оригинальном коде. Но в Android-интеграции
`tun2socks_bridge_run()` вызывается заново при **каждом** перезапуске
движка:

- `startAsnAutoDetect()` в `BypassVpnService.kt` — срабатывает через 1.5
  секунды после включения VPN, определяет оператора СНГ по ASN и, если
  активен пресет `"universal"`, делает `ProxyEngine.stop()` +
  `ProxyEngine.start()` для применения тюнингованного пресета;
- `switchToNextPreset()` — авто-переключение при провале connectivity-теста;
- любой ручной рестарт движка.

При **втором** вызове `bnetwork_initialized`/`btime_global.initialized` уже
равны 1 (никогда не сбрасываются обратно в 0 при остановке/teardown), и
`ASSERT` в badvpn (собранном без `NDEBUG`) вызывает `abort()`. Сигнал
`SIGABRT` убивает **весь процесс приложения** мгновенно — минуя все Kotlin
`try/catch` в `BypassVpnService`/`ProxyEngine` (они оборачивают только
JNI-вызовы с точки зрения Kotlin, но `abort()` в нативном коде завершает
процесс на уровне ОС, а не бросает исключение в JVM).

Именно поэтому пользователь наблюдал не Kotlin-краш с трассировкой стека, а
"тихое" сворачивание приложения: `logcat` в этом случае показывает
`Fatal signal 6 (SIGABRT)` в native-потоке (`tun2socks_jni`/бридж), а не
Java/Kotlin stacktrace.

### 3.3 Исправление

Обе функции сделаны идемпотентными — при повторном вызове просто
возвращаются без побочных эффектов (их тело физически не нужно выполнять
дважды: `BNetwork_GlobalInit()` лишь ставит `SIGPIPE` в игнор,
`BTime_Init()` лишь фиксирует точку отсчёта для относительных таймеров):

```c
// system/BNetwork.c
int BNetwork_GlobalInit (void) {
    if (bnetwork_initialized) { return 1; }
    ...
}

// system/BTime.h
static void BTime_Init (void) {
    #ifndef NDEBUG
    if (btime_global.initialized) { return; }
    #endif
    ...
}
```

### 3.4 Проверка

Написан и прогнан локальный C-репродюсер, in точности имитирующий Android
JNI-сценарий: `tun2socks_bridge_run()` → `tun2socks_bridge_stop()` →
`tun2socks_bridge_run()` (второй раз) на реальном `/dev/net/tun`.

- **До фикса**: второй запуск падал со `SIGABRT`
  (`system/BNetwork.c:54 Assertion failed`, exit code 134). После
  добавления фикса только для `BNetwork.c` обнаружился **второй** такой же
  баг — краш переместился на `system/BTime.h:76`. Оба исправлены.
- **После обоих фиксов**: стресс-тест из **5 циклов** rebuild-run-stop
  подряд (с интервалом 500мс, что даже агрессивнее, чем реальный
  1.5-секундный ASN auto-detect) — `EXIT_CODE=0`, ни одного краша, все 5
  циклов завершились строкой `"tun2socks bridge stopped cleanly"`.

**По факту следующего теста пользователя**: фикс раздела 3 сам по себе НЕ
устранил проблему полностью — приложение всё ещё крашится при нажатии
"Запустить VPN" (уже после появления foreground-уведомления, то есть до
истечения 1.5-секундной задержки ASN auto-detect). Значит есть ещё как
минимум одна причина краша, которая либо специфична для реального
Android/Bionic окружения (не воспроизводится в моём Linux x86_64
репродюсере), либо срабатывает раньше, чем я успел проверить. См. раздел 4.

---

## 4. CrashLogger — сборщик диагностики крашей без adb (v3.7.3)

### 4.1 Проблема диагностики

Пользователь тестирует APK на реальном устройстве без доступа к
`adb logcat` — соответственно, при возникновении краша единственная
доступная информация — визуальное наблюдение ("приложение свернулось").
Без текста ошибки/сигнала/стектрейса дальнейшая охота за багом вслепую
неэффективна: предыдущий фикс (раздел 3) был основан на гипотезе,
подтверждённой локальным репродюсером, но не на реальных логах устройства
— и оказался неполным.

### 4.2 Решение

Добавлен минимальный сборщик диагностики, работающий полностью на стороне
устройства без внешних инструментов:

- **`CrashLogger.kt`** — `Thread.setDefaultUncaughtExceptionHandler()`,
  перехватывает необработанные Kotlin/Java-исключения, дописывает
  стектрейс в файл `filesDir/crash_log_java.txt`, затем передаёт
  управление предыдущему обработчику (штатное поведение сохраняется).
- **`crash_handler.c`/`.h`** — нативный обработчик сигналов `SIGABRT`,
  `SIGSEGV`, `SIGBUS`, `SIGILL`, `SIGFPE`, `SIGTRAP` через `sigaction()`.
  Пишет диагностику (имя сигнала, `si_code`, faulting address, pid/tid) в
  файл `filesDir/crash_log_native.txt` **только через `write()`** — без
  malloc/printf/локалей внутри самого обработчика (это единственные
  async-signal-safe операции, доступные внутри сигнального контекста).
  После записи передаёт управление предыдущему обработчику (обычно
  системный `debuggerd`/`libsigchain`), чтобы стандартный
  crash-reporting/tombstone продолжал работать как раньше.
- **Контрольные точки** (`crash_log_checkpoint()`) расставлены перед
  каждым потенциально опасным нативным вызовом — `ciadpiStart()` перед
  `init_params()`/`run()`, `tun2socksStart()` перед `pthread_create`,
  `tun2socks_thread()` перед `tun2socks_bridge_run()`. Даже если краш
  происходит через сигнал, который наш обработчик не перехватывает
  (например, `SIGKILL` от low-memory killer — его вообще нельзя
  перехватить), по последней записанной строке в файле будет понятно,
  на каком этапе процесс остановился.
- **UI**: `MainActivity` при каждом запуске проверяет оба файла и, если
  есть данные, показывает их в заметной красной карточке в самом верху
  экрана — с кнопками "Копировать" (в буфер обмена) и "Закрыть" (удаляет
  логи). Не требует ADB — пользователь может скопировать текст и вставить
  прямо в чат.

### 4.3 Проверка

Функциональные тесты нативного обработчика (Linux x86_64, gcc):
- Намеренный `*(int*)NULL = 42` (`SIGSEGV`) — обработчик перехватил,
  записал `Signal: SIGSEGV (11)`, `faulting address: 0x0`, корректно
  передал сигнал дальше (процесс завершился стандартным механизмом,
  `exit code 139`).
- Намеренный `abort()` (`SIGABRT`) — аналогично, `Signal: SIGABRT (6)`,
  `exit code 134`.
- Полный сценарий: `crash_handler_install()` → TUN → `tun2socks_bridge_run()`
  → `stop()` — все контрольные точки записались в правильном порядке,
  чисто, без ложных срабатываний.

**По факту следующего теста пользователя (v3.7.3)**: лог краша содержал
ТОЛЬКО одну строку — "crash handler installed" (пишется при запуске
приложения, до нажатия кнопки), после неё абсолютно ничего: ни блока
`NATIVE CRASH`, ни Java-исключения, ни расставленных чекпоинтов
(`ciadpi_thread: entered`, `tun2socks_thread: entered`). Значит сам
CrashLogger не сработал корректно — раздел 5 разбирает найденные причины.

---

## 5. Фикс CrashLogger — почему лог был пустым (v3.7.4)

### 5.1 Баг №1: crash_handler.c компилируется отдельно в каждую .so

`crash_handler.c` — обычный `.c`-файл, добавленный в исходники ОБОИХ
модулей (`ciadpi_jni.so` и `tun2socks_jni.so`) в `CMakeLists.txt`. Это
означает, что после линковки у каждой `.so` — **свой собственный** набор
глобальных переменных (`g_log_fd`, `g_installed` и т.д.), полностью
независимый от другой библиотеки. `ProxyEngine.installCrashHandler()` в
v3.7.3 вызывал JNI-функцию, которая физически находится в `ciadpi_jni.so`
— соответственно:
- В `ciadpi_jni.so` файл лога открылся, `g_log_fd` валиден — строка
  "crash handler installed" (записанная именно оттуда) успешно попала в
  файл.
- В `tun2socks_jni.so` `crash_handler_install()` никогда не вызывался —
  `g_log_fd` там навсегда остался `-1`, и все `crash_log_checkpoint()`
  внутри `tun2socks_thread`/`tun2socksStart` молча ничего не писали (см.
  реализацию: `if (g_log_fd < 0 || !tag) return;`).

Если реальный краш происходит в tun2socks/badvpn-коде (что вероятно,
учитывая, где были найдены другие баги — см. раздел 3), вся диагностика
по этому пути просто терялась.

**Исправление**: добавлена вторая JNI-функция
`installCrashHandlerTun2socks()` в `tun2socks_jni.c`, вызывающая тот же
`crash_handler_install()`, но уже внутри области видимости
`tun2socks_jni.so`. `ProxyEngine.installCrashHandler()` теперь вызывает
обе JNI-функции.

### 5.2 Баг №2: SA_ONSTACK без реального sigaltstack()

`crash_handler_install()` указывал `sa.sa_flags = SA_SIGINFO | SA_ONSTACK`,
но **никогда не вызывал `sigaltstack()`** для регистрации самого
альтернативного стека. Если краш вызван переполнением стека (частый
сценарий в глубоко вложенном C-коде вроде lwIP — рекурсивные вызовы,
большие локальные буферы), обработчик сигнала попытался бы выполниться на
уже испорченном (переполненном) стеке текущего потока и сам немедленно
погиб бы — тихо, без единой записанной строки. Это отлично объясняет
полностью пустой лог: обработчик формально был "установлен", но не мог
физически исполниться при самом вероятном типе краша.

Дополнительная тонкость: `sigaltstack()` — **per-thread** настройка, не
глобальная для процесса. Обработчик устанавливался в главном UI-потоке
(`MainActivity.onCreate()`), но сами краши происходят в отдельных
`pthread`-потоках (`ciadpi_thread`, `tun2socks_thread`) — значит
недостаточно вызвать `sigaltstack()` один раз в UI-потоке, нужно делать
это в каждом рабочем потоке отдельно.

**Исправление**: добавлена функция
`crash_handler_install_altstack_current_thread()`, использующая
`__thread`-локальный буфер (TLS) под аварийный стек каждого потока.
Вызывается в начале `ciadpi_thread()` и `tun2socks_thread()`, до любого
потенциально опасного кода.

### 5.3 Проверка (контрольный эксперимент)

Написана тестовая C-программа с намеренным переполнением стека
(бесконечная рекурсия с локальным буфером) внутри отдельного `pthread`:

- **БЕЗ фикса** (`sigaltstack()` не вызван для потока — воспроизводит
  поведение v3.7.3): `SIGSEGV`, `exit code 139`, но лог-файл **абсолютно
  пуст** — ни одной записанной строки. Ровно то же поведение, что
  наблюдал пользователь на реальном устройстве.
- **С фиксом** (`crash_handler_install_altstack_current_thread()` вызван
  в начале потока): тот же `SIGSEGV`, `exit code 139`, но лог корректно
  содержит `Signal: SIGSEGV (11)`, `si_code: 2`, `faulting address`,
  `pid`/`tid`.

Также перепроверены исходные тесты из раздела 4.3 (nameренный
null-pointer dereference и `abort()`) — оба по-прежнему корректно
перехватываются после добавления `sigaltstack()`.

**Не проверено**: сборка через реальный Android NDK (см. CI) и реальный
краш на устройстве — логика идентична для Linux/Bionic (`sigaltstack()`
и TLS через `__thread` — стандартные POSIX/Bionic-функции, без изменений
на Android), но подтверждение появится только после того, как
пользователь пришлёт содержимое карточки краша из следующего теста.

---

## 6. SIGKILL-гипотеза и checkpoint-трассировка (v3.7.5)

### 6.1 Новые данные от пользователя (устройство OPPO/ColorOS)

После установки v3.7.4 (с исправленным CrashLogger) пользователь сообщил:
- Лог краша теперь содержит ОБЕ строки установки обработчика:
  `ciadpi_jni: crash handler installed` И
  `tun2socks_jni: crash handler installed` — то есть баг №1 из раздела 5
  (установка только в одной .so) действительно исправлен, оба модуля
  готовы перехватывать сигналы.
- После этих строк — снова полная тишина: ни блока `NATIVE CRASH`, ни
  Java-исключения.
- Устройство: OPPO (ColorOS). Приложение исчезает **мгновенно на рабочий
  стол БЕЗ системного диалога** "Приложение остановлено" / "App has
  stopped".
- Настройки батареи для приложения уже стоят "Без ограничений" —
  стандартная оптимизация исключена как причина.

### 6.2 Пересмотр диагноза: SIGKILL

Если бы это был непойманный Kotlin-краш, Android показал бы системный
диалог. Если бы это был native-краш (`SIGABRT`/`SIGSEGV`/...), теперь
корректно установленный обработчик (раздел 5) должен был бы его
перехватить. Ни то, ни другое не произошло — при этом приложение
**мгновенно исчезает без каких-либо следов**.

Это классическая картина **`SIGKILL`** — сигнала, который система (или
прошивка) посылает процессу напрямую через ядро. `SIGKILL` **невозможно
перехватить никаким кодом** — ни `sigaction()` в C, ни
`Thread.UncaughtExceptionHandler` в Java/Kotlin, потому что ядро Linux
принудительно завершает процесс, не давая ему шанса выполнить
какой-либо обработчик.

ColorOS (прошивка OPPO) традиционно известна одним из самых агрессивных
среди Android-прошивок управлением фоновыми процессами — отдельно от
стандартных настроек "Оптимизация батареи" (которые уже проверены и
установлены как "Без ограничений") там есть собственный механизм
"Автозапуск" (Startup Manager / Phone Manager → Privacy Permissions →
Startup Manager), который может принудительно завершать процессы
приложений, не входящих в белый список, особенно VPN-приложений не из
официального AppGallery/Play Store.

### 6.3 Решение: checkpoint-трассировка без нативных сигналов

Поскольку `SIGKILL` невозможно перехватить, единственный способ понять,
**на каком именно шаге** процесс был убит — расставить частые
контрольные точки, которые пишутся на диск СРАЗУ (не буферизуются), и
после следующего запуска посмотреть, какая точка была записана
последней. Добавлено:

- `CrashLogger.checkpoint(context, tag)` — использует
  `RandomAccessFile.fd.sync()` для немедленного сброса на диск (не
  полагается на `File.appendText()`, которая теоретически может быть
  отложена буферизацией ОС), с ограничением размера файла (64 КБ) чтобы
  не расти бесконечно между сеансами без крахов.
- Чекпоинты расставлены по всему пути от нажатия кнопки до полного
  запуска VPN: `MainActivity` (нажатие "Запустить VPN" →
  `VpnService.prepare()` → `onActivityResult` → `startService()`),
  `BypassVpnService` (`onCreate` → `onStartCommand` → `startVpn` →
  `startForeground()` → `runVpn()` → `builder.establish()` →
  `ProxyEngine.start()` → `isNativeTun2socksBuilt()` →
  `runNativeIdleLoop()`).
- Также добавлены системные колбэки `onTaskRemoved()`, `onLowMemory()`,
  `onTrimMemory()` в `BypassVpnService` — если система убивает сервис из
  экрана "Недавние" или из-за нехватки памяти, это будет видно явно и не
  спутается с крахом непосредственно от нажатия кнопки.
- UI: карточка краша в `MainActivity` теперь показывается, даже если оба
  крашлога (Java и Native) пусты, но есть checkpoint-лог — именно этот
  сценарий актуален для `SIGKILL`. Показываются последние 4000 символов
  (а не первые) — самое важное всегда в конце, перед смертью процесса.

### 6.4 Ожидаемый результат следующего теста

Если гипотеза `SIGKILL` верна, checkpoint-лог покажет последнюю
успешно выполненную точку — например, если она обрывается сразу после
`"startVpn: перед startForeground()"`, это укажет на несовместимость
`foregroundServiceType="specialUse"` с ColorOS; если обрывается после
`"runVpn: перед builder.establish()"` — проблема в самом
VPN-туннелировании; если процесс убивается ДО первого чекпоинта вообще
(лог полностью пуст) — это подтвердит, что ColorOS убивает процесс ещё
на старте, до выполнения кода приложения (например, через политику
"не разрешён автозапуск").

В любом из этих случаев программное решение невозможно (нельзя
перехватить/предотвратить `SIGKILL` от системы) — единственный выход:
инструкция для пользователя по добавлению приложения в список разрешённых
в ColorOS Startup Manager / отключению агрессивной оптимизации именно
для этого приложения.

---

## 7. Новая зацепка от реального checkpoint-лога (v3.7.6)

### 7.1 Полученный лог (устройство OPPO/ColorOS)

Пользователь прислал полный checkpoint-лог. Временной анализ показал:

```
t=1783687731970  нажата кнопка "Запустить VPN"
t=1783687734832  onActivityResult (после системного диалога разрешения VPN)
t=1783687734850  BypassVpnService.onCreate()
t=1783687734855  перед startForeground()
t=1783687734859  startForeground() успешно выполнен
t=1783687734862  runVpn: начало
t=1783687734873  перед builder.establish()
t=1783687734895  builder.establish() вернул успех
t=1783687734897  tunFd=164, перед ProxyEngine.start()
t=1783687734898  ProxyEngine.start() вернул true
t=1783687734898  nativeTun2socksBuilt=true
t=1783687734899  перед runNativeIdleLoop()
t=1783687734899  runNativeIdleLoop: вход
                 ... ТИШИНА 5354 мс ...
t=1783687740253  MainActivity.onCreate() — приложение ЗАПУЩЕНО ЗАНОВО (после краша)
```

**Весь путь запуска VPN занял 7 миллисекунд и прошёл идеально** — ни
одной проблемы с `startForeground()`, разрешениями, `establish()` или
самим запуском нативного движка. Краш происходит не при старте, а
**через 5.3 секунды реальной работы**.

### 7.2 Совпадение по времени с ASN auto-detect

5.3 секунды — это подозрительно точно совпадает с `startAsnAutoDetect()`:
`Thread.sleep(1500)` + до `CONNECT_TIMEOUT_MS(2500) + READ_TIMEOUT_MS(2500)
= 5000` мс сетевых таймаутов в `NetworkProfileDetector.detect()` = вписывается
в диапазон 1.5–6.5 секунды, что покрывает наблюдаемые 5.354с.

Если `NetworkProfileDetector` успешно определяет оператора СНГ (по ASN
через ip-api.com), `startAsnAutoDetect()` вызывает **тот же самый
рестарт нативного движка** (`ProxyEngine.stop()` → `sleep(300)` →
`ProxyEngine.start()`), который уже ДВАЖДЫ был причиной найденных и
исправленных крашей (раздел 3: `BNetwork_GlobalInit()`/`BTime_Init()`
one-shot ASSERT). Возможная причина, почему проблема ещё не устранена
полностью: в E2E-тестах (раздел 3.4, стресс-тест 5 циклов) рестарт
проверялся на синтетическом TUN без реального сетевого трафика. На
реальном устройстве в момент рестарта через TUN уже идёт **настоящий
трафик телефона** (фоновые приложения, системные сервисы) — это
принципиально другие условия гонки/состояния lwIP-стека, которые могли
не проявиться в тесте с одним TCP-соединением.

### 7.3 Добавленная трассировка (v3.7.6)

- Детальные чекпоинты внутри `startAsnAutoDetect()`: старт потока → вызов
  `NetworkProfileDetector.detect()` → результат (isp/asn) → решение о
  рестарте → `ProxyEngine.stop()` → `ProxyEngine.start()` — каждый шаг
  теперь отдельная точка.
- `catch (e: Throwable)` вместо `catch (e: Exception)` в этом потоке —
  на случай `Error`-подобных сбоев.
- Периодический тик каждую секунду внутри `runNativeIdleLoop()` — резко
  сужает 5.3-секундное окно неопределённости: если Kotlin-поток успевает
  записать несколько тиков перед смертью, значит JVM была жива всё это
  время (убил либо нативный код, либо системный watchdog ПОКА JVM ещё
  работала); если тиков нет вообще — процесс убит почти мгновенно после
  входа в idle loop, что не укладывается в гипотезу ASN auto-detect
  (сдвигает подозрение обратно к раннему `SIGKILL`).

### 7.4 Ожидаемый результат

- Если лог обрывается сразу после `"startAsnAutoDetect: поток запущен"` —
  проблема в самом потоке/`Thread.sleep()`, маловероятно.
- Если обрывается внутри/сразу после `"вызываем NetworkProfileDetector.detect()"`
  — проблема в сетевом запросе (protect()-сокет, чтение HTTP-ответа) под
  реальной сетевой нагрузкой.
- Если обрывается после `"перед ProxyEngine.stop()"` или
  `"ProxyEngine.stop() вернулся"` или `"перед ProxyEngine.start()"` —
  подтверждает гипотезу о рестарте движка под реальным трафиком как
  корневой причине — тогда нужно искать race condition в
  `tun2socks_bridge_stop()`/`tun2socks_bridge_run()` именно при активных
  TCP-соединениях в момент остановки (не покрыто предыдущими тестами).
- Если несколько тиков `runNativeIdleLoop()` появились ДО обрыва — значит
  ASN auto-detect тут ни при чём, и нужно искать причину в самом
  tun2socks/ciadpi под длительной реальной нагрузкой (возможно, лимит
  памяти/файловых дескрипторов, или race condition, требующий больше
  трафика для проявления).

---

## 8. ASN auto-detect исключён — гипотеза сдвинута к OOM/реальной нагрузке (v3.7.7)

### 8.1 Новый checkpoint-лог (то же устройство OPPO/ColorOS)

```
t=721461  runNativeIdleLoop: вход
t=721462  startAsnAutoDetect: поток запущен, ждём 1500ms
          ... ТИШИНА 6115 мс ...
t=727577  MainActivity.onCreate() — приложение ЗАПУЩЕНО ЗАНОВО
```

Между последним чекпоинтом и перезапуском прошло 6.115 секунды — этого
времени достаточно, чтобы сработали ОБА следующих ожидаемых чекпоинта:
`runNativeIdleLoop: tick=1` (~1с после входа) и
`AsnAutoDetect: вызываем NetworkProfileDetector.detect()` (~1.5с после
старта потока). **Ни один из них не появился в логе.**

### 8.2 Вывод: ASN auto-detect исключён

Процесс умирает менее чем за секунду после входа в `runNativeIdleLoop()`
— раньше, чем поток `AsnAutoDetect` вообще успевает проснуться от
`Thread.sleep(1500)`. Гипотеза раздела 7 (рестарт движка из-за ASN
auto-detect) не подтвердилась и исключена.

### 8.3 Новая гипотеза: OOM/Low Memory Killer под реальной нагрузкой

Правдоподобная причина: сам нативный tun2socks/ciadpi не переживает
**первую волну реального трафика устройства**. `BypassVpnService.runVpn()`
регистрирует маршруты `addRoute("0.0.0.0", 0)` и `addRoute("::", 0)` —
это означает, что ВЕСЬ трафик телефона (десятки фоновых приложений и
системных сервисов, работающих параллельно) мгновенно начинает идти
через TUN в момент установки интерфейса. Это принципиально другая
нагрузка по сравнению с синтетическим E2E-тестом (раздел 3.4), где
проверялось только одно TCP-соединение.

Наиболее вероятный кандидат — Android Low Memory Killer (LMK, cgroup-based
OOM killer), который посылает `SIGKILL` напрямую процессу при нехватке
памяти в системе. Важно: `onLowMemory()`/`onTrimMemory()` — это
ЗАБЛАГОВРЕМЕННЫЕ предупреждения от системы, а не гарантия того, что они
будут вызваны непосредственно перед самим killer'ом (LMK может убить
процесс мгновенно при резком скачке потребления, до того как успеет
дойти очередь до вызова колбэков).

Каждое TCP/UDP-соединение в `SocksUdpClient`/`BSocksClient` открывает
собственный сокет — при резком притоке множества параллельных соединений
(десятки приложений одновременно устанавливают связь при поднятии VPN)
возможен как резкий скачок потребления памяти, так и исчерпание лимита
файловых дескрипторов процесса (`RLIMIT_NOFILE`) — оба сценария способны
привести либо к убийству процесса системой, либо к `abort()`/`SIGSEGV`
внутри самого C-кода при неудачном `socket()`/`malloc()`.

### 8.4 Добавленная трассировка (v3.7.7)

`BypassVpnService.startFastMemoryWatch()` — независимый поток, тикающий
каждые 250мс (в 4 раза чаще предыдущих чекпоинтов), логирующий:

- `Debug.getNativeHeapAllocatedSize()` — потребление native heap;
- `Runtime.totalMemory() - Runtime.freeMemory()` — потребление JVM heap;
- `ActivityManager.MemoryInfo.availMem`/`.lowMemory`/`.threshold` —
  системное состояние памяти с точки зрения LMK;
- количество записей в `/proc/self/fd` — открытые файловые дескрипторы
  процесса (сокеты, файлы).

Если гипотеза OOM/LMK верна, последние записи `mem-watch #N` перед
обрывом лога покажут резкий рост потребления и/или `systemLowMemory=true`
непосредственно перед смертью процесса. Если потребление остаётся
стабильным — гипотеза неверна, и причину нужно искать в race condition
самого C-кода (tun2socks/ciadpi) при реальной многопоточной нагрузке, не
воспроизведённой синтетическим тестом.

Увеличен лимит размера checkpoint-файла с 64KB до 256KB (учитывая
возросшую частоту записи), лимит отображения в UI — с 4000 до 8000
последних символов.

**Не проверено**: реальный результат на устройстве — логика мониторинга
использует стандартные Android API (`Debug`, `ActivityManager`,
`/proc/self/fd`), но подтверждение или опровержение гипотезы появится
только после следующего теста пользователя.

---

## 9. OOM-гипотеза опровергнута данными, добавлены нативные connection-чекпоинты (v3.7.8)

### 9.1 Полученные данные mem-watch (третий checkpoint-лог, то же устройство)

```
mem-watch #1: nativeHeapKb=16551 jvmUsedKb=5645 systemAvailKb=4611696 systemLowMemory=false openFds=183
mem-watch #2: nativeHeapKb=17527 jvmUsedKb=5965 systemAvailKb=4625584 systemLowMemory=false openFds=203  (+20 fds за 255мс)
mem-watch #3: nativeHeapKb=17527 jvmUsedKb=5965 systemAvailKb=4615564 systemLowMemory=false openFds=203  (стабильно)
                ... смерть процесса в окне ~250-500мс после #3 ...
```

### 9.2 Вывод: OOM/Low Memory Killer исключён

- `systemAvailKb` ~4.6 ГБ свободной памяти на протяжении всех трёх замеров;
- `systemLowMemory=false` во всех трёх записях — система НЕ считает
  устройство в состоянии нехватки памяти;
- `nativeHeapKb` вырос всего на ~1 МБ (16551→17527) и затем стабилизировался;
- `jvmUsedKb` стабилен (~5.6-6 МБ) — тривиальное потребление.

Эти данные полностью опровергают гипотезу OOM/LMK из раздела 8 — на
устройстве объективно достаточно памяти, и Android не должен был убивать
процесс по этой причине.

### 9.3 Единственная заметная аномалия: рост числа файловых дескрипторов

`openFds` вырос с 183 до 203 (+20) за 255мс между `mem-watch #1` и `#2`,
затем стабилизировался на 203 — то есть в момент включения VPN произошёл
резкий приток примерно 20 параллельных сетевых соединений (вероятно,
реальные фоновые приложения телефона, установившие TCP/UDP-сессии сразу
после появления маршрута по умолчанию через TUN). Смерть процесса
произошла вскоре после этого (в окне между `mem-watch #3` и ожидаемым, но
отсутствующим `#4`).

Правдоподобная гипотеза: сам факт большого количества (~20+) параллельных
соединений, обрабатываемых tun2socks/lwIP/ciadpi одновременно, триггерит
race condition или логическую ошибку в C-коде, которая не проявляется при
одном тестовом TCP-соединении (как в E2E-тесте раздела 3.4).

### 9.4 Добавленная трассировка (v3.7.8)

Добавлены нативные чекпоинты (через тот же `crash_log_checkpoint()` API,
уже используемый в Kotlin) на каждое новое сетевое соединение:

- **`tun2socks: TCP client #N accepted`** — в `listener_accept_func()`
  (`badvpn/tun2socks/tun2socks.c`), сразу после `num_clients++`. Это
  главная точка приёма нового TCP-соединения из lwIP-стека.
- **`ciadpi: SOCKS5 client fd=N accepted`** — в `on_accept()`
  (`byedpi/proxy.c`), сразу после успешного `add_event()` для нового
  SOCKS5-клиента (то есть после того, как tun2socks подключился к
  локальному SOCKS5-порту 1080 для форвардинга очередного TCP-потока).

Оба чекпоинта скомпилированы под `#ifdef TUN2SOCKS_LIBRARY_MODE` /
`#ifdef ANDROID_APP` соответственно — не влияют на апстримный CLI-режим
badvpn. Include-путь `crash_handler.h` добавлен в `target_include_directories`
статической библиотеки `badvpn_tun2socks` в `CMakeLists.txt`.

Проверено локально: оба изменения компилируются чисто (0 ошибок) отдельно
и в составе полного набора из 58 .c-файлов badvpn/lwIP + пропатченного
`tun2socks.c`.

### 9.5 Ожидаемый результат

При следующем крахе checkpoint-лог покажет последовательность
`"tun2socks: TCP client #1 accepted"`, `"#2"`, ... и/или
`"ciadpi: SOCKS5 client fd=N accepted"` вперемешку с mem-watch записями.
Последнее число N перед обрывом лога укажет точное количество соединений,
обработанных к моменту смерти процесса — это может помочь:

- обнаружить переполнение внутреннего лимита (`MEMP_NUM_TCP_PCB=1024`,
  `DEFAULT_UDPGW_MAX_CONNECTIONS=256` для UDP через SOCKS) — маловероятно
  при ~20 соединениях, но проверяемо;
- увидеть, что краш стабильно происходит на одном и том же N-м клиенте
  (указывает на конкретный race condition, не зависящий от общего числа
  соединений, а триггерящийся определённым паттерном одновременности);
- если ни один из этих чекпоинтов не появится вообще — значит проблема
  ещё раньше, на уровне обработки самих IP-пакетов в
  `device_read_handler_send()`, до этапа `listener_accept_func()`.

## 10. КРИТИЧЕСКИЙ баг найден: сама диагностика "теряла" нативный лог (v3.7.9)

### 10.1 Четвёртый checkpoint-лог (v3.7.8) — что реально пришло

Пользователь прислал полный лог после теста v3.7.8 (нативные connection-
чекпоинты). Нативная (C) часть лога состояла ИЗ ВСЕГО ДВУХ СТРОК:

```
[checkpoint t=1783770713] ciadpi_jni: crash handler installed
[checkpoint t=1783770713] tun2socks_jni: crash handler installed
```

Ни `"ciadpi_thread: entered"`, ни `"tun2socks_thread: entered"`, ни новые
`"tun2socks: TCP client #N accepted"` / `"ciadpi: SOCKS5 client fd=N
accepted"` — при том что Kotlin-сторона лога чётко подтверждает: оба
`pthread_create()` вернули успех (`ProxyEngine.start() вернул true`),
процесс прожил после этого ещё 700+ мс (4 mem-watch тика по 250 мс).

### 10.2 Анализ таймстампа — процесс упавший ≠ процесс, написавший лог

Таймстамп `t=1783770713` (секунды, `time_t`) **точно совпадает** с
последней Kotlin-строкой того же лога:

```
[kt-checkpoint t=1783770713755] MainActivity.onCreate() — приложение запущено
```

(`1783770713755 // 1000 == 1783770713`). То есть обе строки "installed"
были записаны **новым процессом при автоматическом перезапуске после
краха**, а НЕ тем процессом, который упал. Файл нативного лога на момент
самого падения был пуст.

Второй симптом: в присланном Kotlin-логе тоже отсутствует
`"MainActivity.onCreate()"` **перед** первым нажатием кнопки "Запустить
VPN" — при том что этот чекпоинт физически не может не сработать при
каждом создании Activity (это первая строка `onCreate()`). Этот же паттерн
наблюдается и в предыдущем логе (v3.7.7, лог #3 из раздела 9.2) — то есть
это не случайность, а системное поведение пользователя: он видит карточку
с логом предыдущего краха, нажимает **"Закрыть"**, и тут же, **в рамках
того же процесса** (без пересоздания Activity), нажимает "Запустить VPN"
ещё раз.

### 10.3 Найденная причина: `CrashLogger.clearAll()` делал `unlink()` файла с открытым нативным `fd`

`crash_handler.c` открывает `g_log_fd` **один раз на всё время жизни
процесса** (см. `crash_handler_install()`:
`if (g_log_fd < 0 && log_path) g_log_fd = open(path, ...)`), вызывается
из `MainActivity.onCreate() → ProxyEngine.installCrashHandler()`.

Старая версия `CrashLogger.clearAll()` (кнопка "Закрыть" на карточке
краша) делала:
```kotlin
File(context.filesDir, NATIVE_CRASH_FILE).delete()  // unlink()
```
**пока `g_log_fd` этого же процесса всё ещё был открыт на тот же файл.**
`unlink()` удаляет directory entry, но не inode — пока есть хотя бы один
открытый `fd`, файл продолжает существовать "осиротевшим" (открыт, но
недостижим по пути). `crash_handler.c` **никогда не переоткрывает** путь
повторно (проверка `g_log_fd < 0` в начале `crash_handler_install()` не
срабатывает повторно, т.к. эта функция вызывается один раз за жизнь
процесса, а `crash_log_checkpoint()` использует уже открытый `g_log_fd`
напрямую, без повторного `open()`).

В результате: все последующие `crash_log_checkpoint()`-вызовы из
`ciadpi_thread`/`tun2socks_thread` (маркеры входа в поток, новые TCP/
SOCKS5 connection-чекпоинты) успешно проходят системный `write()` —
никаких ошибок, но пишут в НЕДОСТИЖИМЫЙ по пути inode. Когда процесс
умирает (краш/`SIGKILL`), `fd` закрывается ядром, и данные orphan-инода
пропадают безвозвратно. Пользователь при следующем запуске читает файл
**по пути** — там уже новый (пустой) файл, созданный текущим
`open(O_CREAT)` нового процесса.

**Важно**: Kotlin-сторона (`checkpoint()`/`appendToFile()`) этому багу НЕ
подвержена — она открывает файл **заново по пути на каждую отдельную
запись** (`RandomAccessFile(f, "rw")` / `File.appendText()`), поэтому
"самоисцеляется" после удаления — отсюда и разница в поведении между
Kotlin- и C-логами, которая долго маскировала эту проблему (Kotlin-лог
выглядел нормально, а C-лог таинственно пропадал).

### 10.4 Подтверждение экспериментом

Локальный C-эксперимент в песочнице (`gcc`, обычная файловая система):

```c
int fd = open(path, O_CREAT|O_WRONLY|O_APPEND, 0600);
write(fd, "session-A: installed\n", ...);
unlink(path);                 // эквивалент File.delete()
write(fd, "session-B: doomed\n", ...);   // пишется в fd успешно...
close(fd);                    // ...но после close() данные исчезли НАВСЕГДА,
                               // cat по пути показывает пустоту всё время
                               // после unlink()
```
подтвердил: после `unlink()` при открытом `fd`, `cat` по пути **сразу**
перестаёт показывать файл, и все последующие записи в `fd`
недостижимы. Контрольный эксперимент с `truncate(path, 0)` вместо
`unlink()` показал противоположный результат: после `truncate(0)` файл
по пути остаётся видимым (пустым), и все последующие `write()` в тот же
`fd` **корректно появляются** при чтении по пути — то есть `truncate()`
не разрывает связь между открытым `fd` и путём, а `unlink()` разрывает.

### 10.5 Исправление (v3.7.9)

`CrashLogger.clearAll()` теперь **не удаляет** `NATIVE_CRASH_FILE`, а
обрезает его до нуля **по тому же пути/inode**:
```kotlin
RandomAccessFile(f, "rw").use { it.setLength(0) }  // ftruncate(), не unlink()
```
`JAVA_CRASH_FILE` и `CHECKPOINT_FILE` по-прежнему удаляются через
`delete()` — им это безопасно, т.к. ни `Thread.UncaughtExceptionHandler`,
ни `CrashLogger.checkpoint()` не держат долгоживущий открытый `fd`, они
открывают файл заново на каждую запись.

### 10.6 Что это означает для всей предыдущей диагностики

Гипотезы про OOM/LMK (раздел 9), ASN auto-detect (раздел 8) и SIGKILL
(раздел 7) были построены на данных, где **весь native crash-лог был
систематически пуст** из-за этого бага диагностики — то есть мы никогда
не видели, доходил ли процесс вообще до `ciadpi_thread`/
`tun2socks_thread` или падал раньше/позже них, и никогда не видели
реальный `SIGSEGV`/`SIGABRT`/... стектрейс, если сигнальный обработчик
успевал сработать. Kotlin-чекпоинты (mem-watch, tick) остаются
достоверными (они не зависят от этого бага), но они показывают только
"снаружи" (со стороны JVM) — сам процесс `ciadpi`/`tun2socks` мог
падать по СИГНАЛУ (`SIGSEGV`/`SIGABRT`/...), а не только по `SIGKILL`, и
мы бы это увидели впервые именно сейчас, после фикса v3.7.9.

### 10.7 Ожидаемый результат от следующего теста

С этим фиксом при следующем крахе (если он есть) native crash-лог должен
наконец показать РЕАЛЬНОЕ содержимое: маркеры входа в оба потока,
connection-чекпоинты (сколько именно TCP/SOCKS5-соединений успело
пройти), и, если крах вызван перехватываемым сигналом (`SIGSEGV`/
`SIGABRT`/`SIGBUS`/`SIGILL`/`SIGFPE`/`SIGTRAP`) — полный блок
`=== NATIVE CRASH ===` с именем сигнала, `si_code`, faulting address,
pid/tid. Если после этого фикса native-лог **всё ещё** пуст — это будет
сильным аргументом в пользу чистого `SIGKILL` (не перехватываемого в
принципе), и тогда единственный источник данных — Kotlin-чекпоинты
(которые теперь тоже должны корректно накапливаться между попытками,
если пользователь тестирует без нажатия "Закрыть" между двумя запусками
VPN подряд).

## 11. НАЙДЕНА И ИСПРАВЛЕНА РЕАЛЬНАЯ ПРИЧИНА КРАША: SIGSEGV из-за неинициализированного `params.mempool` (v3.7.10)

### 11.1 Четвёртый checkpoint-лог (после фикса v3.7.9) — впервые виден реальный сигнал

Фикс v3.7.9 (truncate вместо unlink native crash-лога) сработал: пользователь
прислал лог, где **впервые за всю историю диагностики** нативный
crash-лог содержит реальные данные вместо пустоты:

```
[checkpoint] tun2socks: TCP client #1 accepted
[checkpoint] ciadpi: SOCKS5 client fd=157 accepted
[checkpoint] tun2socks: TCP client #2 accepted
[checkpoint] tun2socks: TCP client #3 accepted
[checkpoint] tun2socks: TCP client #4 accepted
[checkpoint] ciadpi: SOCKS5 client fd=173 accepted
[checkpoint] ciadpi: SOCKS5 client fd=174 accepted
[checkpoint] ciadpi: SOCKS5 client fd=175 accepted
[checkpoint] ciadpi: SOCKS5 client fd=176 accepted
Signal: SIGSEGV (11)
si_code: 1
faulting address: 0x2
pid: 5138 tid: 5761
```

Это произошло **дважды подряд** (в обеих попытках — до и после
автоперезапуска процесса), с идентичным `faulting address: 0x2` оба
раза — значит это не гонка потоков или зависящий от таймингов баг, а
**полностью детерминированная логическая ошибка**.

### 11.2 Анализ: faulting address 0x2 указывает точно на `struct mphdr.cmp_type`

`0x2` — крайне характерный адрес: NULL-указатель + смещение 2 байта.
Локальный C-эксперимент (`offsetof`) подтвердил:

```c
struct mphdr {
    unsigned short flags;      // offset 0
    unsigned char  cmp_type;   // offset 2   <--!!!
    size_t         count;
    struct elem   *root;
};
```

`offsetof(struct mphdr, cmp_type) == 2` — точное совпадение.

В byedpi (`extend.c`) есть кэш desync-параметров по IP назначения
(`cache_get()`/`cache_add()`), используемый через `find_dp()` при
обработке КАЖДОГО нового TCP-соединения (`connect_hook()` →
`on_trigger()`). Обе функции читают `hdr->cmp_type`, где `hdr =
params.mempool`.

### 11.3 Корневая причина: `params.mempool` никогда не инициализировался в нашем `ciadpi_jni.c`

Апстримный byedpi (`main.c: parse_args()`) ВСЕГДА создаёт мемпул перед
стартом:
```c
params.mempool = mem_pool(MF_EXTRA, CMP_BITS);
if (!params.mempool) { uniperror("mem_pool"); return -1; }
```

Но наш `ciadpi_jni.c` **не использует** `parse_args()` — он строит
`struct params` вручную в собственной функции `init_params()` (нужно для
передачи конфигурации из Kotlin через JNI, а не через CLI-аргументы).
`init_params()` делает `memset(&params, 0, sizeof(params))` и никогда не
вызывал `mem_pool()` — значит `params.mempool` оставался `NULL` на
протяжении ВСЕЙ истории проекта (с версии v3.7, когда ciadpi_jni.c был
впервые написан).

Как только код доходит до пути, использующего кэш (не на первом же
соединении — зависит от внутренней логики `find_dp()`/`on_trigger()` в
`extend.c`, что и объясняет, почему падение происходит именно на 4-м
клиенте, а не на 1-м), происходит `hdr->cmp_type` при `hdr == NULL` →
`SIGSEGV` по адресу `0x2`.

### 11.4 Подтверждение экспериментом (точное совпадение с реальным устройством)

Написан минимальный C-репродюсер с реальным `sigaction()`-обработчиком
`SIGSEGV`, точно копирующий структуру `mphdr` и код `mem_get()`:

```c
struct mphdr *mempool = NULL;      /* как в старом init_params() */
sink = mempool->cmp_type;          /* точная копия mem_get() из mpool.c */
```

Результат локального запуска (`gcc -O0`, Linux x86_64):
```
Signal: SIGSEGV (11)
si_code: 1
faulting address: 0x2
```

**Точное побайтовое совпадение** с логом с реального устройства
(OPPO, ColorOS, ARM64) — разные архитектуры, но одинаковый баг даёт
идентичный faulting address, что подтверждает: причина не зависит от
платформы, это чистая логическая ошибка в C-коде.

Контрольный тест с фиксом (`mempool = mem_pool(MF_EXTRA, CMP_BITS)`
перед тем же вызовом `mem_get_buggy()`) — код отрабатывает без падения:
```
before call
AFTER call: NO CRASH, fix works!
```

### 11.5 Исправление (v3.7.10)

`app/src/main/jni/ciadpi_jni.c`, `init_params()`:
```c
if (!params.mempool) {
    params.mempool = mem_pool(MF_EXTRA, CMP_BITS);
    if (!params.mempool) {
        LOGE("OOM: mem_pool (mempool)");
    }
}
```

`cleanup_params()` — добавлено освобождение через `mem_destroy()`, т.к.
движок может быть перезапущен несколько раз за время жизни процесса
(`ProxyEngine.stop()+start()` в `startAsnAutoDetect()`/
`switchToNextPreset()`) — без этого каждый рестарт создавал бы новый
`mempool` через `mem_pool()`, а старый (со всем накопленным кэшем)
просто тёк бы в памяти:
```c
if (params.mempool) {
    mem_destroy(params.mempool);
    params.mempool = NULL;
}
```

`mpool.h` уже подключён транзитивно через `params.h` (строка
`#include "mpool.h"`), поэтому `mem_pool()`, `mem_destroy()`,
`MF_EXTRA`, `CMP_BITS` доступны в `ciadpi_jni.c` без дополнительных
include — компиляция подтверждена локально (0 ошибок и 0 warnings,
кроме ожидаемых из-за упрощённых фейковых `jni.h` заголовков в
песочнице).

### 11.6 Что это означает для всей истории диагностики

Обе главные гипотезы предыдущих версий (v3.7.7: OOM/Low Memory Killer;
рабочая гипотеза v3.7.8: race condition в C-коде под параллельной
нагрузкой) были **ошибочными** — реальная причина существовала с самого
начала (с v3.7, когда `ciadpi_jni.c` был впервые написан) и была
детерминированной логической ошибкой (забытая строка инициализации), а
не связанной с нагрузкой, памятью или таймингом. Мы не могли увидеть её
раньше исключительно из-за отдельного, не связанного бага в самой
diagnostике (v3.7.5-v3.7.8: native crash-лог систематически терялся
из-за `unlink()` при открытом `fd`, исправлено только в v3.7.9).

Это объясняет и "детерминированность" происходящего на 4-м клиенте в
обоих логах (до и после автоперезапуска) — при одинаковом трафике на
старте VPN (одни и те же системные сервисы/приложения телефона
подключаются в одном и том же порядке) код детерминированно доходит до
проблемного пути `find_dp()`/`cache_get()` на одном и том же по счёту
соединении.

### 11.7 Ожидаемый результат от следующего теста

Если гипотеза верна, приложение должно теперь **не падать** при
включении VPN — весь трафик (TCP и UDP через SOCKS5 UDP ASSOCIATE)
должен обрабатываться штатно, независимо от количества параллельных
соединений. Если краш всё же повторится — native crash-лог (благодаря
фиксу v3.7.9) покажет НОВЫЙ сигнал/faulting address, что укажет на
следующую, отдельную проблему.

---

## Итог

| Пункт roadmap | Статус | Что осталось |
|---|---|---|
| tun2socks native | ✅ Реализовано, E2E-протестировано на Linux, зелёный CI на Android NDK | Ручной тест на реальном устройстве |
| Краш при рестарте tun2socks (BNetwork/BTime one-shot ASSERT) | ✅ Исправлено (см. раздел 3) | Не единственная причина — краш всё ещё есть, см. раздел 9 |
| CrashLogger — диагностика без adb (v3.7.3) | ⚠️ Реализовано, но не работало корректно | См. следующую строку |
| Фикс CrashLogger (v3.7.4) | ✅ 2 бага найдены и исправлены, подтверждено контрольным экспериментом | Оба обработчика работают, но не поймали реальный краш — см. раздел 6 |
| SIGKILL-гипотеза + checkpoint-трассировка (v3.7.5) | ✅ Реализовано | Checkpoint-лог получен — см. раздел 7 |
| ASN auto-detect рестарт под реальным трафиком (v3.7.6) | ❌ Гипотеза исключена (раздел 8) | — |
| OOM/Low Memory Killer под реальной нагрузкой (v3.7.7) | ❌ Гипотеза опровергнута данными (раздел 9) | — |
| Нативные connection-чекпоинты (v3.7.8) | ✅ Добавлены и протестированы компиляцией | Не сработали из-за бага диагностики — см. следующую строку |
| Баг диагностики: `clearAll()` терял native crash-лог через unlink() при открытом fd (v3.7.9) | ✅ Найден, подтверждён экспериментом, исправлен (truncate вместо delete) | Впервые получен реальный native crash-лог — см. следующую строку |
| **РЕАЛЬНАЯ ПРИЧИНА КРАША: `params.mempool` никогда не инициализировался → SIGSEGV на cache_get()/cache_add() (v3.7.10)** | ✅ Найдена, подтверждена локальным репродюсером (точное совпадение faulting address), исправлена | Ждём подтверждения на реальном устройстве |
| ECH | План задокументирован | Сборка BoringSSL-ECH под NDK, JNI-обёртка, HTTPS/SVCB-парсинг в DohResolver — отдельный релиз v4.0 |
| IPv6 TCP split (Kotlin fallback) | ✅ Сделано | — |
| DoH-клиент (перехват DNS) | ✅ Сделано (DnsInterceptor.kt) | — |
| Пресеты СНГ + ASN автоопределение | ✅ Сделано | Ручная калибровка TTL под реальные измерения в полевых условиях |
| IPv6 в ciadpi SOCKS5 назначениях | ✅ Сделано (dual-stack baddr), проверено функциональным тестом | — |
| Split-tunnel self-exclusion bug | ✅ Исправлено | — |

